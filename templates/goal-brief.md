---
type: task
status: active
priority: high
tags:
  - goal
  - campaign
description: >-
  ONE-LINE TRUE SUCCESS BAR (falsifiable). Continuable handoff is not completion.
  Compaction summary is not warrant.
---

# {Goal title}

> This file is the **goal record and the goal prompt**. Feed the body (or the paste block) to a fresh session. Update it in place as the campaign evolves. **Tips on disk outlive context** — design every surface for compactification survival.

## One-line goal (true success)

{A falsifiable domain predicate — the only completion bar besides an explicit stop by the goal's owner.}

## Explicit failure modes (do not repeat)

1. …
2. …

## Standing doctrine

### Obstacle protocol

On any block: name it → fan out ≥3 attack vectors → ≥1 that rewrites a foundation → ≥1 deliberately high-risk path with a named kill criterion → run the experiments → loop. Do not complete the campaign on a local kill.

### Honesty package

- Standing blocks (do not re-elevate): …
- What is **not** success: packaging, volume, soft-KEEP chains, iteration counts, **compaction/summary residue**, …

### Pressure package

- Sortes: mandatory on multi-arm waves after kickoff; correct catalog; tag the draws; end artifacts with `not surfaced:` trails. **Re-draw after any boundary.**
- Adversarial-review cadence: …
- Re-arm when kill-pressure is sick, context depth is high, **or post-boundary orientation fails** — checkpoint, end the session, re-post this brief.
- Outcome-shape check: if draws vary but the wave skeleton freezes, force a falsifying arm or change the engine class.

### Surfaces (named tips)

| Surface | Path | Tip role |
|---------|------|----------|
| Search log | … | System of record for attempts; tip = frontier + ordered next moves |
| Status ledger | … | Elevation registry + open tip + standing blocks |
| Exhibits | … | Code / formal proofs / tests, cited by the tips |

### Compactification package (required for class A)

**Orientation packet (read in this order after a cold start or a compaction):**

1. This brief (true success + refuse list)
2. Status ledger tip
3. Search-log tip
4. Exhibit paths cited by the tips only
5. Pressure note (if kept separately)

**Pre-boundary (before a compaction, pause, or session end):** write the search-log tip + status tip + pressure line; land durable work. Refuse "the summary will keep it."

**Post-boundary:** re-read the orientation packet from disk. Refuse summary-as-warrant for bars, standing blocks, tips, and kills. If tips lag or pressure is sick → hard re-arm; do not soft-continue into more work.

## Companion skills

(Skill paths shown for the Claude Code layout — substitute your harness's skills directory.)

- Load `/prokope` (this doctrine).
- Multi-agent work: `/fabrica`.
- Decorrelation: `/sortes` — research waves use the ideation catalog:
  `bun .claude/skills/sortes/bin/pick.ts --catalog .claude/skills/sortes/lenses-ideation --n <arms>`
- Density/telos checks: ISDA checkpoints C0–C5 on the brief and on wave deltas.
- Lifecycle: keep this record's status current in your task tracker.

## Acceptance

**True success only** completes the goal. A session end **or a soft compaction** produces continuable tips, an updated search log, and a version-control landing; the goal stays active unless its owner pauses it. Summary residue is never completion evidence.

## Goal-prompt block

See `templates/goal-prompt-block.md` — paste into fresh sessions.
